    <h2>User info</h2>
    <%#= attachment_image_tag user, :profile_image_id, :fill, 50, 50, fallback: "no_image.jpg"%>
    <%= attachment_image_tag user, :profile_image_id, size: "100x100", fallback: "no_image.jpg"%>
    <!--プロフ画像-->
    <table class="table table-hover table-inverse">
      <thead>
    <!--<tr>-->
    <!-- <th>Name</th>-->
    <!--<th><</th>-->
    <!--</tr>-->
    <!--<tr>-->
    <!--<th>Introduction</th>-->
    <!--</tr>-->
      <tbody>
        <tr>

     <th>Name</th>
    <th><%= user.name %></th>
    </tr>
    <tr>
    <th>Introduction</th>
    <th><%= user.introduction %></th>
    </tr>
    <tr>
      <th>follows</th>
      <th>
        <%= link_to user.followings.count, user_followings_path(user) %>
      </th>
    </tr>
    <tr>
      <th>followers</th>
      <th><%= link_to user.followers.count, user_followers_path(user) %></th>
    </tr>

    </tbody></table>
    <div>
      <% if current_user != user %>
  　　　　<% if current_user.following?(user) %>
　        <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, class:"btn btn-danger" %>
　         <div>
              <!--edit画面-->
              <%= link_to "chat", chats_show_path(user), class: "btn btn-success" %>
              <%#= link_to "chat", chats_show_path(chat.id) %>
            </div>
        <% else %>
          <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class:"btn btn-success" %>
        <% end %>
      <% end %>
    </div>



      <div class="row">
        <%= link_to "", edit_user_path(user), class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user" %>
      </div>