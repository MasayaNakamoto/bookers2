<div class="post-body">
  <p>Title：<%= @book.title %></p>
  <p>説明：<%= @book.opinion %></p>
  <p>投稿日時：<%= @book.created_at.strftime('%Y/%m/%d') %></p>
  <% if @book.user == current_user %>
    <%= link_to "削除", post_image_path(@book), method: :delete %>
</div>